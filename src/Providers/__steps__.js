/**
 * context api
 * 1. create context (export)
 * 2. create provider
 * 3. set context value
 * 4. to access the value : useContext hook
 * **/ 

/**
 * 1.use set the provider to the main.jsx
 * 2.inside the provider access the children props and then use
 * ***/ 

/**
 * WAY TO SET UP PRIVATE ROUTE
 * 1.to create a private route at first i have create a route then it put in the private route 
 * 2.the main route is work as the child in the private route
 * **/ 